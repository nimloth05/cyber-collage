export class ApplyWorldChanges {
  execute(): void {
    // remove all agents from Scene (Group Object of ThreeJs)
    // Iterate over grid any apply each position of agent [row, column, layer, stack?] to agent. Add with new position to scene
    // set Grid and agentList as values to AgentCube
  }
}
