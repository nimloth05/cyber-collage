import {LineSegments, Mesh} from "three";

export class FoundationGrid extends LineSegments {
  // can differentiate from other ohter Object3D using obj.constructor.name
}

export class FoundationHover extends LineSegments {
  // can differentiate from other ohter Object3D using obj.constructor.name
}

export class HelperMeshes extends Mesh {
  // can differentiate from other other Object3D using obj.constructor.name
}
